[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    error::DeprecationWarning
    # Raised inside starlette's TestClient import, not by this project.
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
